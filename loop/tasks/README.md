# tasks/

Task packets (`T-001.md`, `T-002.md`, ...) are generated per milestone by `/plan-milestone` and cleared/renumbered from `T-001` at the start of each new milestone. This file is a placeholder so the directory exists in git before the first milestone is planned.
