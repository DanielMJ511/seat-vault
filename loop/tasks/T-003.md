# T-003 — POST /api/bookings/{id}/cancel controller wiring

Milestone: M6 (#11)   Status: pending

## Description

Add `POST /api/bookings/{id}/cancel` to `BookingController`, calling the `BookingService.cancel` method built in T-001. Mirror the existing `POST /api/bookings/{id}/confirm` endpoint's structure (status code, principal handling, response body via `toResponse(...)`/`BookingResponse`).

Depends on T-001 (service method must exist first).

## Acceptance criteria

- `POST /api/bookings/{id}/cancel` on a CONFIRMED booking the caller owns: 200, returns the updated `BookingResponse` with `status: CANCELLED`.
- Same 404/409 behavior as `BookingService.cancel` (T-001), surfaced correctly through `GlobalExceptionHandler`.

## Relevant conventions

- Controllers call Services only.
- Errors flow through `ApiException` → `GlobalExceptionHandler`, never an ad hoc `ResponseEntity` error body.

## Files likely touched

- `src/main/java/com/seatvault/seat_vault/controller/BookingController.java`

## Constraints from LESSONS.md

(carried in verbatim by /orchestrate at spawn time)
