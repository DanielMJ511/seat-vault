# T-004 — Integration + concurrency tests for list/detail/cancel

Milestone: M6 (#11)   Status: pending

## Description

Depends on T-001, T-002, T-003. Add tests covering the issue's Verification section, following the existing patterns in `BookingServiceTest` (Mockito unit tests) and `BookingIntegrationTest` (Testcontainers, `MockMvc`, real Postgres/Redis):

- Unit tests (`BookingServiceTest`): cancel on not-found/not-owned booking → 404; cancel on non-CONFIRMED (PENDING, FAILED, already-CANCELLED) → 409; cancel on CONFIRMED → seats released, status CANCELLED, verify mock interactions (no side effects on the rejected paths).
- Integration tests (`BookingIntegrationTest`):
  - List/detail/cancel happy paths via `MockMvc` + JWT, matching the existing fixture-building style in `@BeforeEach`.
  - Cancel-of-non-CONFIRMED rejected with 409 end-to-end.
  - Cancellation verified to make the seat immediately holdable again: cancel a booking, then have a different user successfully create a Hold on the now-freed `EventSeat`.
  - A double-cancel concurrency test: two concurrent `cancel` calls on the same CONFIRMED booking. Follow the existing `concurrentReleaseAndCreateFromHoldOnSameHoldResolvesConsistently`-style pattern: `ExecutorService` + `CountDownLatch` start gate, `Future.get(30, TimeUnit.SECONDS)`, assert exactly one call succeeds (200, booking CANCELLED, seats AVAILABLE) and the other gets 409 — not "no crash," the exact win/loss split.

## Acceptance criteria

All of the issue's `## Verification` scenarios pass, plus the concurrency scenario the grilling session identified.

## Relevant conventions

- Integration tests run against real Postgres/Redis via Testcontainers `@ServiceConnection` — never mocked. Docker must be running.
- Follow the existing fixture-construction and JWT-auth patterns already in `BookingIntegrationTest` rather than inventing new test infrastructure.

## Files likely touched

- `src/test/java/.../BookingServiceTest.java`
- `src/test/java/.../BookingIntegrationTest.java`

## Constraints from LESSONS.md

(carried in verbatim by /orchestrate at spawn time)
