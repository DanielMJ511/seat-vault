# Local development profile. Activate with SPRING_PROFILES_ACTIVE=dev (or
# --spring.profiles.active=dev) when running against docker-compose's
# Postgres/Redis to also load deterministic seed data (demo venues, events,
# and login accounts) alongside the base schema.
spring.flyway.locations=classpath:db/migration,classpath:db/seed
